# Spec: Packaging + 0.2.0 Release Prep (Pass 3b of 3 — final)

**Status:** Rev 4 — done-criteria metadata line aligned with Task A
(matplotlib under plot and dev extras, never unconditional)
**Author:** Claude.ai planning session
**Implementer:** Claude Code (sole author — Codex is suggest-only, never writes)
**Baseline:** main at `7532669` with CI green (263 tests, 100% coverage)
**Scope:** matplotlib → optional `[plot]` extra with lazy import (BREAKING for
plot users), sdist slimming, numpy dev bound, README refresh with reproducible
output blocks, version 0.2.0 + CHANGELOG cut. Claude Code prepares and
commits; PyPI upload, tag, and GitHub Release are executed by Ani afterward.

## Read-first instructions (mandatory, in order)

1. Read this spec completely.
2. Read `pyproject.toml`, `src/pybenford/__init__.py`,
   `src/pybenford/visualization.py` (imports only), `README.md`,
   `CHANGELOG.md`, `tests/test_regression.py`, `examples/` listing,
   `.github/workflows/ci.yml` (context only — do not edit).
3. Baseline gate from `pybenford-dev`: `pip install -e ".[dev]"`, checkout
   assert, seven-pin heredoc, `pytest -q` (expect 263 passed, 100.00%).
   STOP if red.

## Anti-hallucination guardrails

- Implement exactly the tasks below. Other issues found: report, don't fix.
- Do NOT edit `.github/workflows/ci.yml`.
- Do NOT upgrade numpy locally (stays 2.4.6).
- Do NOT run `twine upload`, `git tag`, `git push`, or `gh release`.
- Every README output block must be regenerated by RUNNING the exact
  producer code specified in Task D — never hand-typed.
- Any deviation from the prescribed lazy-import mechanics is stop-and-review.

## Task A — matplotlib becomes the `[plot]` extra (BREAKING)

1. `pyproject.toml`:
   - Remove `matplotlib>=3.5.0` from `[project] dependencies`.
   - Add `[project.optional-dependencies] plot = ["matplotlib>=3.5.0"]`.
   - Add `matplotlib>=3.5.0` AND `numpy>=2.2,<2.5` to `dev` extras (numpy
     rationale: 2.5.x stubs use `type` statements unparseable under the
     `python_version = "3.10"` mypy target; 3.10 CI resolves 2.2.x, local
     3.13 resolves 2.4.x — both in-bound. One-line comment in pyproject).

2. Lazy import in `src/pybenford/__init__.py` — exact mechanics:

   ```python
   from typing import TYPE_CHECKING

   if TYPE_CHECKING:
       # Type checkers see the real signatures; runtime stays lazy.
       from pybenford.visualization import (
           plot_digit_test,
           plot_distortion_factor,
           plot_mantissa_arc,
           plot_ordered_mantissas,
           plot_summation,
           plot_z_scores,
       )

   _PLOT_EXPORTS = frozenset({
       "plot_digit_test", "plot_distortion_factor", "plot_mantissa_arc",
       "plot_ordered_mantissas", "plot_summation", "plot_z_scores",
   })

   def __getattr__(name: str) -> object:
       if name in _PLOT_EXPORTS:
           try:
               from pybenford import visualization
           except ModuleNotFoundError as exc:
               if exc.name == "matplotlib" or (
                   exc.name is not None and exc.name.startswith("matplotlib.")
               ):
                   raise ImportError(
                       f"{name} requires matplotlib. "
                       'Install the plot extra: pip install "pybenford[plot]"'
                   ) from exc
               raise  # unrelated failure — never relabel as missing matplotlib
           attr = getattr(visualization, name)
           globals()[name] = attr  # cache: subsequent access skips __getattr__
           return attr
       raise AttributeError(f"module 'pybenford' has no attribute {name!r}")

   def __dir__() -> list[str]:
       return sorted(set(globals()) | _PLOT_EXPORTS)
   ```

   `__all__` unchanged (the six plot names stay). Documented consequence:
   `from pybenford import *` on an analysis-only install raises the helpful
   ImportError — intended behavior, stated in README. Non-plot imports stay
   eager and untouched. This must pass mypy strict.

3. New tests (`tests/test_lazy_import.py`) — enumerated; report exact final
   suite count (263 + your additions):

   a. IN-PROCESS error branch (this is what keeps coverage at 100% —
      pytest-cov 7 does not measure subprocesses without extra config, which
      we are not adding): use a meta-path blocker fixture that
      (i) removes `matplotlib*` and `pybenford.visualization` from
      `sys.modules`,
      (ii) ALSO saves and pops `pybenford.__dict__["visualization"]` — pytest
      collection has already bound the submodule as a package attribute, and
      `from pybenford import visualization` would return that stale
      attribute without ever consulting the finder (gate-1 reproduced this;
      without step (ii) the blocker receives zero calls),
      (iii) installs an `importlib.abc.MetaPathFinder` raising
      ModuleNotFoundError(name="matplotlib") for matplotlib imports,
      (iv) clears the lazy cache (`pybenford.__dict__.pop(name, None)` for
      plot names). Then `pybenford.__getattr__("plot_digit_test")` must
      raise ImportError whose message contains `pybenford[plot]`.
      Restore sys.modules, the package attribute, and the cache in teardown.
   b. IN-PROCESS sentinel: same blocker but raising
      ModuleNotFoundError(name="some_other_module") from the visualization
      import — assert the error propagates UNCHANGED (message does not
      mention pybenford[plot]). Proves unrelated failures are not relabeled.
   c. SUBPROCESS cold-import: `python -c "import sys, pybenford;
      assert 'matplotlib' not in sys.modules"` exits 0. (Subprocess because
      pytest collection imports tests/test_visualization.py → matplotlib;
      in-process cannot prove cold behavior.)
   d. SUBPROCESS cold lazy-load success: single subprocess asserting
      matplotlib not in sys.modules after `import pybenford`, then
      `pybenford.plot_digit_test` resolves callable, then matplotlib IS in
      sys.modules.
   e. `pybenford.plot_digit_test is pybenford.visualization.plot_digit_test`
      (and the cache: second access finds it in `pybenford.__dict__`).
   f. `pybenford.nonexistent_name` raises AttributeError naming it.
   g. `dir(pybenford)` contains all six plot names before any plot access.
   h. Wheel metadata invariant (in-suite), matching the DECLARED extras
      exactly: `importlib.metadata.requires("pybenford")` must show
      (i) NO unconditional matplotlib entry — every matplotlib entry carries
      an extra marker, and both `extra == "plot"` and `extra == "dev"`
      matplotlib entries exist; (ii) exactly one UNCONDITIONAL numpy entry
      (`>=1.22.0`) plus the bounded `extra == "dev"` numpy entry;
      (iii) an unconditional scipy entry.

## Task B — slim the sdist (exclude ALL of examples/)

```toml
[tool.hatch.build.targets.sdist]
exclude = ["examples/"]
```

(Whole directory: the CSV and notebook are the payload, and
`examples/test_census_population.py` references the excluded CSV — shipping
it broken is worse than not shipping it. `.claude/` needs no entry; hatch
excludes VCS-ignored paths by default.)

README consequence (do in Task D): any README link to `examples/…` becomes an
absolute GitHub URL
(`https://github.com/anirbanmukherjee2709/pybenford/blob/main/examples/…`)
so sdist readers aren't pointed at absent files.

Verification (real output in summary): `rm -rf dist/ build/ &&
python -m build`; `tar -tzf dist/*.tar.gz` must show NO `examples/` entry
and MUST include `src/`, `tests/`, `docs/`, README, LICENSE, CHANGELOG,
pyproject; report both artifact sizes; `twine check dist/*` passes. Then
unpack the sdist to a temp dir and, in a clean venv with the built wheel
installed WITH the plot extra (`pip install "<wheel>[plot]"` — the full
suite collects tests/test_visualization.py, which imports matplotlib) plus
pytest/pytest-cov/hypothesis, run `pytest <unpacked>/tests -q` with
`-p no:cacheprovider`: expect all tests pass with the census regression
reported as SKIPPED (its skipif guard sees no CSV in the unpacked tree).
If the unpacked-tree run needs `--no-cov` because coverage config targets
the src layout, use it and say so.

## Task C — clean-venv wheel validation (release gate, real output)

From the built wheel (Task B artifacts):

1. Fresh temp venv A: `pip install <wheel>` (NO extras) → `python -c
   "import pybenford; print(pybenford.BenfordAnalysis)"` works;
   `python -c "import matplotlib"` FAILS (not installed);
   `python -c "import pybenford; pybenford.plot_digit_test"` raises the
   helpful ImportError mentioning pybenford[plot].
2. Fresh temp venv B: `pip install "<wheel>[plot]"` → `python -c
   "import pybenford; print(pybenford.plot_digit_test)"` works.

Paste the actual commands and output for both venvs.

## Task D — README refresh (reproducible output blocks)

1. Install section: `pip install pybenford` (analysis);
   `pip install "pybenford[plot]"` (visualizations). State the 0.2.0
   breaking change for plot users, the `import *` behavior, and the
   Python ≥3.10 floor.
2. Claims: 263+ tests (use the exact final count), 100% line coverage.
3. Output blocks — the README has exactly two literal output blocks; their
   current data provenance is undefined (the displayed n=3,195 corresponds
   to ALL census rows including 51 state aggregates). Recut BOTH on the
   canonical dataset, and show the producer code in the README immediately
   above each block so any reader can reproduce:
   - Canonical data: `examples/data/co-est2025-alldata.csv`, stdlib csv,
     `encoding="latin-1"`, filter `row["SUMLEV"].strip() == "050"`, column
     `POPESTIMATE2025` → 3,144 county records (matches the regression suite
     and the published Medium article).
   - Block 1 producer: `print(BenfordAnalysis(data).first_digit())`.
   - Block 2 producer: `print(BenfordAnalysis(data).first_two_digits())`
     (the existing second block is a First Two Digits Test display).
   - REPRODUCIBILITY NOTE (state it in the README next to the producer
     code): the dataset ships in the GitHub repository, not in the PyPI
     package — link the raw-file GitHub URL so sdist/PyPI readers can
     download it or clone the repo.
   - Paste ONLY actual captured output. Report old n → new n and any
     value/wording changes.
4. Fix the `examples/` links per Task B (absolute GitHub URLs).

## Task E — cut 0.2.0 in version and CHANGELOG

1. `pyproject.toml`: `version = "0.2.0"`.
2. `CHANGELOG.md`: retain an empty `## [Unreleased]` heading at top; rename
   the populated section to `## [0.2.0] - <ACTUAL DATE>` using `date +%F` at
   commit time — do not hard-code a stale date. Add the 3b items with the
   BREAKING entries first: matplotlib → `[plot]` extra with lazy import
   (upgrade note: `pip install "pybenford[plot]"`), sdist no longer ships
   examples. Existing Unreleased content (Python ≥3.10 floor, Pass 2 fixes,
   Pass 3a changes) stays under 0.2.0.

## Validation gate

Post-edit `pip install -e ".[dev]"`, checkout assert, seven-pin heredoc PLUS
the numpy bound check
(`v = tuple(map(int, version("numpy").split(".")[:2])); assert (2,2) <= v < (2,5)`),
then: `ruff check src tests` · `ruff format --check src tests` · `mypy src` ·
`pytest -q` (report exact count; coverage must be 100.00% — the in-process
error-branch test is what makes this reachable), then Tasks B/C verifications.
AFTER all verification: `rm -rf dist/ build/` so no pre-commit artifacts
linger — the RELEASE build is rebuilt by Ani from the final approved commit,
not from these verification artifacts. Paste real output throughout.

## Commit plan

1. `docs: add pass 3b spec (packaging, 0.2.0 release prep)` — this file.
2. `feat!: matplotlib as optional [plot] extra; slim sdist; 0.2.0 release prep`
   — Tasks A–E, gate-validated. Do not push; do not tag; do not publish.

## Handoff to Ani (executed after gate-2 diff review approves, not by Claude Code)

Push → CI green → then from the approved commit: `rm -rf dist/ build/ &&
python -m build && twine check dist/*` → `twine upload dist/*` →
`pip install pybenford==0.2.0` verification → `git tag -a v0.2.0` → push tag
→ GitHub Release. (Step-by-step guidance comes from Claude.ai at handoff.)

## Done criteria

- Typed surface intact (TYPE_CHECKING imports), dir() complete, error path
  relabels ONLY missing-matplotlib, coverage 100.00% including the error
  branch in-process.
- Wheel metadata: matplotlib only under the optional extras (plot and dev),
  never unconditional; clean-venv A/B validation passes with pasted output.
- Sdist excludes examples/ entirely; unpacked-sdist test run passes with the
  regression test SKIPPED; README links repaired; both output blocks carry
  their producer code and real 3,144-county output.
- Version 0.2.0; CHANGELOG cut with actual date, empty Unreleased retained.
- `git status --porcelain` empty; exactly two new commits; not pushed;
  dist/ and build/ removed after verification.
